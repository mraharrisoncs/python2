level=input("level?")
if level = "H":
    lineIn=input("Enter a high-level statement:")
    if lineIn="if x == y":
        lineOut="CMP R1,R2"
    elif:
        # complete this section
        
else:
    # complete this section
